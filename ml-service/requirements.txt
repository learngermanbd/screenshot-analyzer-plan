# Core
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# Image Processing
Pillow==11.1.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Color Extraction
scikit-learn==1.6.0

# OCR — EasyOCR (CPU-friendly, works on HF Spaces free tier)
easyocr==1.7.2

# AI Analysis
httpx==0.28.1

# Optional: PaddleOCR (better accuracy, but ~2GB install + GPU recommended)
# paddleocr==2.9.1
# paddlepaddle==2.6.2

# Optional: Florence-2 (requires GPU + model download)
# transformers==4.47.0
# torch==2.5.1
